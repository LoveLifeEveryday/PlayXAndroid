<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xcynice.playxandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".module.mine.activity.AboutUsActivity"></activity>
        <activity android:name=".module.mine.activity.SettingActivity" />
        <activity android:name=".module.search.activity.SearchActivity" />
        <activity android:name=".module.mine.activity.AboutMeActivity" />
        <activity android:name=".module.mine.activity.OpenSourceActivity" />
        <activity android:name=".module.mine.activity.MineCollectActivity" />
        <activity android:name=".module.mine.activity.ShareArticleActivity" />
        <activity android:name=".module.mine.activity.MineShareActivity" />
        <activity android:name=".module.mine.activity.CoinRankActivity" />
        <activity android:name=".module.mine.activity.CoinActivity" />
        <activity android:name=".module.login.activity.LoginActivity" />
        <activity android:name=".module.tree.tree_grandson.TreeGrandsonActivity" />
        <activity android:name=".module.article_detail.ArticleDetailActivity" />
        <activity android:name=".module.main.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 适配华为刘海屏 -->
        <meta-data
            android:name="android.notch_support"
            android:value="true" /> <!-- 适配小米刘海屏 -->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />
    </application>

</manifest>